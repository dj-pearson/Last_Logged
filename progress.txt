# Ralph Progress Log
Started: 2026-03-24

## Codebase Patterns
- Xcode project uses MVVM folder structure: Models/, ViewModels/, Views/{Home,Detail,Onboarding,Settings,Components}/, Services/, Widget/, Utilities/
- SPM dependencies declared in project.pbxproj via XCRemoteSwiftPackageReference (supabase-swift, purchases-ios, TelemetryDeck SwiftClient)
- App Group identifier: group.com.pearsonmedia.lastlogged (configured in LastLogged.entitlements)
- Bundle identifier: com.pearsonmedia.lastlogged
- iOS 17+ deployment target, SwiftUI lifecycle
- Empty directories use .gitkeep files for git tracking
- SwiftData models: TrackerItem, CompletionLog, TrackerCategory in Models/ folder
- SyncStatus enum (synced/pending/conflict) in Models/SyncStatus.swift — used by TrackerItem
- ModelContainer configured with App Group shared store in LastLoggedApp.swift (URL: appGroupURL/LastLogged.sqlite)
- TrackerItem links to TrackerCategory via categoryId (UUID), not a SwiftData @Relationship
- CompletionLog links to TrackerItem via trackerItemId (UUID), not a SwiftData @Relationship
- DataSeeder in Utilities/ handles first-launch seed data; called from LastLoggedApp .onAppear
- Use FetchDescriptor + fetchCount for lightweight existence checks in SwiftData
- ViewModels are @Observable classes that take ModelContext in init; views pass modelContext from SwiftUI environment
- Color(hex:) extension on Color lives in Views/Home/HomeView.swift — converts "#RRGGBB" hex strings to SwiftUI Color
- ContentView is a thin wrapper that renders HomeView; new top-level navigation goes through HomeView
- Highest pbxproj IDs: A1...000D (BuildFile), A2...000D (FileRef)
- TrackerRowView takes `onLog: () -> Void` closure — must be provided when constructing the row
- `import UIKit` needed for haptic feedback (UIImpactFeedbackGenerator) — SwiftUI alone doesn't expose it
- AddTrackerView takes HomeViewModel (not ModelContext) and uses its createItem method; present as .sheet from HomeView
- AddTrackerView supports edit mode via optional `editingItem: TrackerItem?` parameter — pre-populates all fields from existing item
- TrackerDetailViewModel is @Observable, takes ModelContext + TrackerItem — prep for US-024 (completion history)
- Use `.sheet(item:)` with TrackerItem for edit sheet — @Model classes conform to Identifiable via PersistentModel
- Swipe actions use `.swipeActions(edge: .trailing, allowsFullSwipe: false)` for non-destructive-by-default archive
- Context menu on rows: `.contextMenu` with Edit and Archive options
- CategoryTemplates keyed by category name string — match templates to categories via `category.name`
- HomeView toolbar + button is a Menu with "New Tracker" and "Add from Templates" — add new creation options there
- NotificationService.shared is the singleton for local notifications — call `rescheduleAllNotifications(modelContext:)` after any tracker mutation
- Notification uses `UNCalendarNotificationTrigger` at 9 AM on due dates; 64-item limit handled by overdue priority sort
- Widget extension target: LastLoggedWidgetExtension (com.pearsonmedia.lastlogged.widget), shares models via duplicate PBXBuildFile entries
- WidgetDataProvider.shared in LastLogged/Widget/ — creates its own ModelContainer from App Group URL; compiled in both targets
- OverdueItem struct + UrgencyLevel enum live in WidgetDataProvider.swift — use for widget display data
- Widget extension files are in LastLoggedWidget/ directory (sibling to LastLogged/)
- Use `@Environment(\.widgetFamily)` to switch widget layouts by size; `containerBackground(.fill.tertiary, for: .widget)` for iOS 17+ backgrounds
- OverdueItem.sampleItems provides placeholder/preview data for widget gallery — use `context.isPreview` in getSnapshot
---

## 2026-03-24 - US-001
- What was implemented: Xcode project scaffolding with MVVM folder structure, SPM dependencies, App Group capability, and placeholder ContentView
- Files changed:
  - LastLogged.xcodeproj/project.pbxproj (full Xcode project with build settings, SPM deps, entitlements)
  - LastLogged/LastLoggedApp.swift (SwiftUI @main app entry point)
  - LastLogged/ContentView.swift (placeholder view with app branding)
  - LastLogged/Info.plist (minimal, using GENERATE_INFOPLIST_FILE)
  - LastLogged/LastLogged.entitlements (App Group: group.com.pearsonmedia.lastlogged)
  - LastLogged/Assets.xcassets/ (AccentColor, AppIcon placeholders)
  - .gitkeep files in all MVVM subdirectories
- **Learnings for future iterations:**
  - pbxproj uses 24-char hex IDs with prefix pattern: A1=BuildFile, A2=FileRef, A3=Group, A4=BuildPhase, A5=Target, A6=Project, A7=BuildConfig, A8=ConfigList, A9=PackageRef, AA=PackageDep
  - When adding new Swift files, must add PBXFileReference + PBXBuildFile + add to parent PBXGroup children
  - Project is on Windows — cannot run xcodebuild for typecheck verification; structure verified manually
  - GENERATE_INFOPLIST_FILE = YES means most Info.plist keys come from build settings (INFOPLIST_KEY_* prefix)
---

## 2026-03-24 - US-002
- What was implemented: SwiftData @Model classes for TrackerItem, CompletionLog, and TrackerCategory, plus SyncStatus enum. ModelContainer configured in app entry point with App Group shared store.
- Files changed:
  - LastLogged/Models/SyncStatus.swift (new - SyncStatus enum with synced/pending/conflict cases)
  - LastLogged/Models/TrackerItem.swift (new - @Model with all required fields)
  - LastLogged/Models/CompletionLog.swift (new - @Model with id, trackerItemId, completedAt, notes)
  - LastLogged/Models/TrackerCategory.swift (new - @Model with id, name, iconName, colorHex, sortOrder, isDefault)
  - LastLogged/LastLoggedApp.swift (updated - ModelContainer with Schema and App Group container URL)
  - LastLogged.xcodeproj/project.pbxproj (updated - added 4 new source files to project)
  - LastLogged/Models/.gitkeep (removed - no longer needed)
- **Learnings for future iterations:**
  - Models use UUID-based foreign keys (categoryId, trackerItemId) instead of @Relationship — keeps it simple for sync
  - SyncStatus is a separate file since it's an enum used by TrackerItem (not a @Model itself)
  - ModelContainer fallback: if App Group URL is nil, uses default ModelConfiguration
  - Highest pbxproj IDs after this story: A1...000A (BuildFile), A2...000A (FileRef)
---

## 2026-03-24 - US-003
- What was implemented: DataSeeder utility class that seeds 6 default TrackerCategory records on first launch. Seeding is idempotent (checks fetchCount == 0 before inserting). Called from LastLoggedApp via .onAppear on ContentView.
- Files changed:
  - LastLogged/Utilities/DataSeeder.swift (new - static seedDefaultCategoriesIfNeeded method)
  - LastLogged/LastLoggedApp.swift (updated - calls DataSeeder on ContentView .onAppear)
  - LastLogged.xcodeproj/project.pbxproj (updated - added DataSeeder.swift to project)
  - LastLogged/Utilities/.gitkeep (removed - replaced by real file)
- **Learnings for future iterations:**
  - Use FetchDescriptor + fetchCount for lightweight existence checks (avoids loading all objects)
  - DataSeeder is a struct with static methods — no state needed, keeps it simple
  - Default categories: Home Maintenance (#4A90D9, house.fill), Health & Wellness (#E74C3C, heart.fill), Car Care (#F39C12, car.fill), Personal Care (#9B59B6, person.fill), Social & Family (#2ECC71, person.2.fill), Pet Care (#1ABC9C, pawprint.fill)
  - Highest pbxproj IDs after this story: A1...000B (BuildFile), A2...000B (FileRef)
---

## 2026-03-24 - US-004
- What was implemented: HomeViewModel as @Observable class with CRUD operations, category grouping, and overdue sorting. Fetches non-archived TrackerItems and TrackerCategories from SwiftData. Supports creating items (with auto-incrementing sortOrder per category), archiving (soft delete), and updating sort order. Exposes `itemsByCategory` grouped computed property and `itemsSortedByOverdue` computed property using overdue score ratio (elapsed time / reminder interval).
- Files changed:
  - LastLogged/ViewModels/HomeViewModel.swift (new - @Observable ViewModel with fetch, create, archive, sort operations)
  - LastLogged.xcodeproj/project.pbxproj (updated - added HomeViewModel.swift to project)
- **Learnings for future iterations:**
  - HomeViewModel takes ModelContext in init — views should pass `modelContext` from the SwiftUI environment
  - Overdue scoring: ratio of elapsed time to reminder interval (>1.0 = overdue); items with no interval get lowest score; never-completed items with interval get highest score
  - After mutations, call save() then fetchItems() to keep the observable state in sync
  - Highest pbxproj IDs after this story: A1...000C (BuildFile), A2...000C (FileRef)
---

## 2026-03-24 - US-005
- What was implemented: HomeView with category-grouped scrollable List, empty state using ContentUnavailableView, navigation title "Last Logged", and toolbar add (+) button. TrackerRowView shows item icon, name, and placeholder for elapsed time. ContentView updated to render HomeView. Color(hex:) extension added to convert category colorHex to SwiftUI Color for section headers.
- Files changed:
  - LastLogged/Views/Home/HomeView.swift (new - HomeView, TrackerRowView, Color hex extension)
  - LastLogged/ContentView.swift (updated - now renders HomeView instead of placeholder)
  - LastLogged.xcodeproj/project.pbxproj (updated - added HomeView.swift to project)
  - LastLogged/Views/Home/.gitkeep (removed - replaced by real file)
- **Learnings for future iterations:**
  - ContentUnavailableView (iOS 17+) is the idiomatic SwiftUI empty state pattern — use Label for icon+title, description for subtitle, actions for CTA buttons
  - HomeViewModel is initialized lazily via @State in the view's onAppear using the @Environment modelContext
  - Category section headers use Label(name, systemImage:) with foregroundStyle colored by category colorHex
  - TrackerRowView currently shows "—" as elapsed time placeholder — US-007 will replace with real elapsed time display
  - The add button sets showingAddTracker state but no sheet is presented yet — US-008 will implement AddTrackerView
  - Highest pbxproj IDs after this story: A1...000D (BuildFile), A2...000D (FileRef)
---

## 2026-03-24 - US-006
- What was implemented: One-tap event logging with haptic feedback and undo toast. Added logCompletion/undoLog methods to HomeViewModel with LogUndoInfo struct for undo state. Updated TrackerRowView with a prominent green checkmark.circle.fill log button. Added UIImpactFeedbackGenerator (medium) on tap. Added a transient toast overlay with "Logged! Undo" that auto-dismisses after 4 seconds. Undo deletes the CompletionLog and reverts lastCompletedAt.
- Files changed:
  - LastLogged/ViewModels/HomeViewModel.swift (added LogUndoInfo struct, logCompletion, undoLog methods)
  - LastLogged/Views/Home/HomeView.swift (added log button to TrackerRowView, haptic feedback, toast overlay with undo)
- **Learnings for future iterations:**
  - `import UIKit` is needed alongside SwiftUI for UIImpactFeedbackGenerator — SwiftUI doesn't re-export UIKit haptic classes
  - Use `Task { @MainActor in try? await Task.sleep(for:) }` for cancellable toast timers — cleaner than Timer.scheduledTimer in SwiftUI
  - LogUndoInfo holds SwiftData model references (CompletionLog, TrackerItem) — these remain valid after fetchItems() since the same ModelContext manages them
  - TrackerRowView now takes an `onLog: () -> Void` closure — future stories modifying TrackerRowView must include this parameter
  - No new files were added, so no pbxproj changes needed for this story
  - Highest pbxproj IDs unchanged: A1...000D (BuildFile), A2...000D (FileRef)
---

## 2026-03-24 - US-007
- What was implemented: Elapsed time display with color-coded urgency in TrackerRowView. Replaced the "—" placeholder with actual elapsed time using RelativeDateTimeFormatter. Added urgency color coding: green (<75% of reminder interval), yellow (75-100%), red (>100% / overdue). "Never logged" shown for items with no completion history. Items with no reminder interval use neutral .secondary color. Never-logged items with a reminder interval show red (overdue).
- Files changed:
  - LastLogged/Views/Home/HomeView.swift (updated TrackerRowView with elapsedTimeText and urgencyColor computed properties, static RelativeDateTimeFormatter)
- **Learnings for future iterations:**
  - Use a static RelativeDateTimeFormatter on the view struct to avoid repeated allocations — it produces localized strings like "3 days ago", "2 weeks ago"
  - Urgency color logic: ratio = elapsed / (intervalDays * 86400); green < 0.75, yellow 0.75–1.0, red > 1.0
  - Elapsed time auto-updates because fetchItems() is called on .onAppear and after logCompletion/undoLog — no additional timer needed for MVP
  - No new files added, no pbxproj changes needed
  - Highest pbxproj IDs unchanged: A1...000D (BuildFile), A2...000D (FileRef)
---

## 2026-03-24 - US-008
- What was implemented: Custom tracker creation form (AddTrackerView) with name text field, category Picker, optional reminder interval with days/weeks/months toggle, and a searchable SF Symbol grid picker (SFSymbolPickerView). Form validates name is non-empty before enabling Save. Cancel dismisses without saving. Default icon is "checkmark.circle". HomeView updated to present AddTrackerView as a sheet from the + toolbar button and empty state CTA.
- Files changed:
  - LastLogged/Views/Home/AddTrackerView.swift (new - AddTrackerView form with ReminderUnit enum, SFSymbolPickerView with 60+ common symbols in searchable grid)
  - LastLogged/Views/Home/HomeView.swift (updated - added .sheet(isPresented:) to present AddTrackerView)
  - LastLogged.xcodeproj/project.pbxproj (updated - added AddTrackerView.swift to Home group and Sources phase)
- **Learnings for future iterations:**
  - AddTrackerView takes HomeViewModel directly (not ModelContext) — reuses the existing ViewModel's createItem method
  - SFSymbolPickerView uses a static list of ~60 common SF Symbols in a LazyVGrid with 5 columns; search filters by symbol name substring
  - ReminderUnit enum handles days/weeks/months conversion to days (weeks*7, months*30)
  - Category Picker uses `Optional(category.id)` as tag to match `@State private var selectedCategoryId: UUID?`
  - The form is presented as a sheet from HomeView — the viewModel is passed through, so new items appear immediately on dismiss
  - Highest pbxproj IDs after this story: A1...000E (BuildFile), A2...000E (FileRef)
---

## 2026-03-24 - US-009
- What was implemented: Edit and archive tracker functionality. Added swipe-to-archive gesture with destructive style and confirmation dialog on tracker rows. Added context menu (long-press) with Edit and Archive options. Modified AddTrackerView to support edit mode via optional `editingItem` parameter — pre-populates name, category, reminder interval, and icon from existing TrackerItem. Added `updateItem` method to HomeViewModel. Created TrackerDetailViewModel as @Observable class managing single item state. Edit sheet presented via `.sheet(item:)` on HomeView.
- Files changed:
  - LastLogged/ViewModels/HomeViewModel.swift (added updateItem method)
  - LastLogged/ViewModels/TrackerDetailViewModel.swift (new - @Observable class with archiveItem, updateItem, save)
  - LastLogged/Views/Home/AddTrackerView.swift (refactored to support edit mode with pre-populated fields, custom init with State initializers, reverse reminder unit conversion)
  - LastLogged/Views/Home/HomeView.swift (added swipe-to-archive, context menu, edit sheet, archive confirmation dialog)
  - LastLogged.xcodeproj/project.pbxproj (added TrackerDetailViewModel.swift to ViewModels group and Sources phase)
- **Learnings for future iterations:**
  - Use `State(initialValue:)` in custom init to pre-populate @State vars from passed data — works because sheet creates fresh view instances
  - Reverse reminder unit: `days % 30 == 0` → months, `days % 7 == 0` → weeks, else days
  - `.confirmationDialog(presenting:)` pattern is cleaner than manual Binding for optional-driven alerts
  - `.sheet(item:)` works with @Model classes since PersistentModel conforms to Identifiable
  - Multiple `.sheet` modifiers on same view work in iOS 17+ when triggered by different bindings
  - Highest pbxproj IDs after this story: A1...000F (BuildFile), A2...000F (FileRef)
---

## 2026-03-24 - US-010
- What was implemented: Pre-built category templates with starter items. Created CategoryTemplates utility defining starter items per category with name, iconName, and reminderIntervalDays matching all PRD requirements. Created TemplatePickerView with category-grouped list, checkbox selection, select/deselect all, and bulk-add via HomeViewModel.createItem. Updated HomeView toolbar + button to use a Menu with "New Tracker" and "Add from Templates" options. Added "Browse Templates" button to empty state.
- Files changed:
  - LastLogged/Utilities/CategoryTemplates.swift (new - TemplateItem struct, CategoryTemplates static dictionary with all 6 categories)
  - LastLogged/Views/Home/TemplatePickerView.swift (new - template picker with checkbox selection, bulk-add, select/deselect all)
  - LastLogged/Views/Home/HomeView.swift (updated - toolbar + changed to Menu, added template picker sheet, added Browse Templates to empty state)
  - LastLogged.xcodeproj/project.pbxproj (updated - added 2 new files to project)
- **Learnings for future iterations:**
  - CategoryTemplates uses static let dictionary keyed by category name string — templates are matched to categories by name, not ID
  - TemplateItem uses `let id = UUID()` initialized once in the static dictionary — IDs are stable across computed property accesses within a session
  - TemplatePickerView reuses HomeViewModel.createItem for each selected template — no bulk-create method needed since createItem handles sortOrder auto-increment
  - HomeView toolbar now uses Menu for + button — future stories adding more creation options should add to this Menu
  - Color(hex:) extension in HomeView.swift is accessible from TemplatePickerView.swift since both are in the same module
  - Highest pbxproj IDs after this story: A1...0011 (BuildFile), A2...0011 (FileRef)
---

## 2026-03-24 - US-011
- What was implemented: NotificationService singleton class handling all UNUserNotificationCenter interactions. Requests permission on first tracker with a reminder interval. Schedules UNCalendarNotificationTrigger for each tracker at 9:00 AM on the due date. Notification content uses "It's been [X days] since your last [item name]. Time to log it!" format. Includes a "Log Now" UNNotificationAction button. Handles the 64 pending notification limit by prioritizing overdue items first, then soonest-due. Reschedules all notifications after every create, log, update, archive, and undo operation via HomeViewModel.
- Files changed:
  - LastLogged/Services/NotificationService.swift (new - singleton with permission, scheduling, reschedule, category registration)
  - LastLogged/ViewModels/HomeViewModel.swift (updated - added scheduleNotifications() calls after all mutations)
  - LastLogged.xcodeproj/project.pbxproj (updated - added NotificationService.swift to Services group and Sources phase)
  - LastLogged/Services/.gitkeep (removed - replaced by real file)
- **Learnings for future iterations:**
  - NotificationService.shared is the singleton — accessed from ViewModels via `NotificationService.shared.rescheduleAllNotifications(modelContext:)`
  - `UNNotificationAction` with `.foreground` option brings the app to foreground when tapped — use userInfo["trackerItemId"] to identify which tracker
  - `UNCalendarNotificationTrigger` uses DateComponents (year, month, day, hour, minute) — non-repeating for specific dates
  - Overdue items get scheduled for tomorrow 9 AM; future items get scheduled for their due date at 9 AM
  - The 64-notification limit is handled by sorting all items by overdue priority and taking the top 64
  - `rescheduleAllNotifications` removes all pending then re-adds — simpler than tracking individual notification changes
  - Permission is requested lazily (only when items with reminders exist), not at app launch
  - Highest pbxproj IDs after this story: A1...0012 (BuildFile), A2...0012 (FileRef)
---

## 2026-03-24 - US-012
- What was implemented: App Group container and shared SwiftData store for widgets. Created WidgetDataProvider utility in LastLogged/Widget/ that reads TrackerItems from the shared App Group SwiftData store and returns top N most overdue items sorted by urgency. Created widget extension target (LastLoggedWidgetExtension) with its own entitlements, build configurations, and source files. Widget extension compiles shared model files (TrackerItem, CompletionLog, TrackerCategory, SyncStatus) and WidgetDataProvider. Placeholder widget views included for US-013 to build upon.
- Files changed:
  - LastLogged/Widget/WidgetDataProvider.swift (new - WidgetDataProvider singleton with makeSharedModelContainer, getTopOverdueItems, OverdueItem struct, UrgencyLevel enum)
  - LastLoggedWidget/LastLoggedWidgetBundle.swift (new - @main widget bundle entry point)
  - LastLoggedWidget/LastLoggedWidget.swift (new - placeholder widget with TimelineProvider, basic entry view)
  - LastLoggedWidget/LastLoggedWidget.entitlements (new - App Group: group.com.pearsonmedia.lastlogged)
  - LastLogged.xcodeproj/project.pbxproj (updated - added widget extension target, embed phase, dependency, build configs)
  - LastLogged/Widget/.gitkeep (removed - replaced by real file)
- **Learnings for future iterations:**
  - Widget extension target type is `com.apple.product-type.app-extension` with SKIP_INSTALL = YES
  - Widget extension needs its own PBXBuildFile entries for shared model files (same FileRef, different BuildFile IDs)
  - Widget bundle identifier follows pattern: `com.pearsonmedia.lastlogged.widget`
  - WidgetDataProvider.shared creates its own ModelContainer using the App Group URL — independent from the main app's container
  - OverdueItem struct and UrgencyLevel enum are in WidgetDataProvider.swift — shared between main app and widget
  - Widget extension LD_RUNPATH_SEARCH_PATHS needs `@executable_path/../../Frameworks` for app extension context
  - PBXCopyFilesBuildPhase with dstSubfolderSpec = 13 is used for "Embed App Extensions"
  - PBXContainerItemProxy + PBXTargetDependency link widget extension as dependency of main app
  - Highest pbxproj IDs after this story: A1...001B (BuildFile), A2...0017 (FileRef)
---

## 2026-03-24 - US-013
- What was implemented: Small and medium home screen widgets with proper size-differentiated views. Small widget (2x2) shows the single most overdue item with name, elapsed time, icon, and urgency color indicator dot. Medium widget (4x2) shows top 3 most overdue items in a list with urgency dots, icons, names, and elapsed time. Added empty state with app icon and "Open Last Logged to start tracking" message. Used `.containerBackground(.fill.tertiary, for: .widget)` for iOS 17+ widget background. Added sample data for widget gallery placeholders and previews. Removed `.systemLarge` from supported families (US-014 will add it back). Used `StaticConfiguration` (no intent needed for MVP).
- Files changed:
  - LastLoggedWidget/LastLoggedWidget.swift (rewritten - SmallWidgetView, MediumWidgetView, MediumWidgetRowView, urgency helpers, sample data extension on OverdueItem)
  - prd.json (updated - US-013 passes: true)
- **Learnings for future iterations:**
  - Use `@Environment(\.widgetFamily)` in the entry view to switch between widget size layouts
  - `containerBackground(.fill.tertiary, for: .widget)` is the iOS 17+ way to set widget backgrounds — replaces the older `.background()` approach
  - `context.isPreview` in getSnapshot tells you if the widget is shown in the gallery — return sample data for better UX
  - Private free functions (not methods) work fine for shared utility logic across widget view structs
  - Widget supportedFamilies should only list sizes with proper implementations — add `.systemLarge` when US-014 is done
  - No new files added, no pbxproj changes needed — only modified existing LastLoggedWidget.swift
  - Highest pbxproj IDs unchanged: A1...001B (BuildFile), A2...0017 (FileRef)
---
